<?php


$letter = array('Hi ', ' dear', 'Friend ', ' tomato');
var_dump($letter);



function format(array $list = array()) {
	foreach ($list as $key => array()_{
	    $list[key] = strtolower(trim($list));
	return impload(', ', $list);
}
$result = $format($letter);
var_dunmp($result);